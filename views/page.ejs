<br />
<div>
    <% if (!isFirstPage) { %>
    <span class="prepage"><a title="上一页" href="?pageNo=<%= pageNo-1 %>">上一页</a></span>
    <% } %>
    共<%= totalCount%>条，共<%= totalPage%>页，每页<%= pageSize%>条，当前是第<%= pageNo%>页
    <% if (!isLastPage) { %>
    <span class="nextpage"><a title="下一页" href="?pageNo=<%= pageNo+1 %>">下一页</a></span>
    <% } %>
</div>